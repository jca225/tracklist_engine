# Ground-truth scaffold for Two Friends - Big Bootie Mix Vol. 26
# date_played: 2025-06-05
#
# Fill in set_start_s, set_end_s, version_tag, and ref_start_s per
# track. version_tag ∈ {instrumental, acappella, full}. Delete rows
# the DJ didn't actually play. cue_seconds is the scraped cue — a
# hint for where set_start_s usually lands, not a hard anchor.
#
# ref_start_s is MANDATORY — seconds into the reference where the DJ
# first dropped in. For most tracks it's 0 (played from the start).
# For loops/cutups, set is_loop: true and add a ref_segments list
# with one entry per iteration; see bigbootie11_ground_truth.yaml
# for an example.
set_id: 1d9zwh49
source: ableton_session
annotated_by: TBD
tracks:
  - track: "Nirvana — Smells Like Teen Spirit"
    track_id: gp7b7p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Dillon Francis, Julian Jordan — The Sound"
    track_id: 7wf9fh6p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Nelly — Hot In Herre"
    track_id: 73sc2mf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "David Guetta — Titanium"
    track_id: 1z9gbpxx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Metro Station — Shake It"
    track_id: 2r0xsukf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Sabrina Carpenter — Taste"
    track_id: hgkynljp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Kendrick Lamar, SZA — All The Stars"
    track_id: 2wlqhyhx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Skrillex — Bangarang"
    track_id: 8mlx3hf    # scraped cue: 202s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Justin Bieber — Beauty And A Beat"
    track_id: sv2tlsf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Benny Benassi pres. The Biz — Satisfaction"
    track_id: 9    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Tinashe — Nasty"
    track_id: u27mdt5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Benny Benassi pres. The Biz — Satisfaction"
    track_id: 9m3kgf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Benny Benassi pres. The Biz — Satisfaction"
    track_id: d1bmvpf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "David Guetta vs. Benny Benassi — Satisfaction"
    track_id: 29drltz5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "The Rolling Stones — (I Can't Get No) Satisfaction"
    track_id: 1rtsgd0p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Tate McRae — 2 Hands"
    track_id: 15    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "CHRYSTAL — The Days"
    track_id: g00hz0dp    # scraped cue: 309s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Alex Warren — Ordinary"
    track_id: wv0b4g8x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Creed — One Last Breath"
    track_id: 13cp63mx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Skrillex, Hamdi, Taichu, OFFAIAH — Push"
    track_id: fqrszyt5    # scraped cue: 387s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Charli xcx — Guess"
    track_id: 5kwzg1ff    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Cloonee, InntRaw, Young M.A — Stephanie"
    track_id: s5zcyxlf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "MC4D, Łaszewo, gavn! — Be Here With Me Now"
    track_id: vz2xf8sf    # scraped cue: 456s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Travis Scott — Butterfly Effect"
    track_id: j5n74nx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Clean Bandit — Symphony"
    track_id: y44tybf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Vincent — Surrounded"
    track_id: 5kxhcc8p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "nimino — I Only Smoke When I Drink"
    track_id: vpqwbrw5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "AC/DC — T.N.T."
    track_id: 1zghq7bp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "MK, Dom Dolla — Rhyme Dust"
    track_id: 26p190rx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "T.I. — Bring Em Out"
    track_id: 3jzkr4x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "AYOR — Crime Day"
    track_id: wht7j95    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Black Eyed Peas — My Humps"
    track_id: 1b1fd17f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Rentz, Bayze — Highs And Lows"
    track_id: 2301bp15    # scraped cue: 616s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Fetty Wap — Again"
    track_id: cfs99cp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "MGMT — Electric Feel"
    track_id: 5603fp5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Gracie Abrams — That's So True"
    track_id: sh9v4qs5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Knock2 — hold my hand"
    track_id: 1up23zxf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Mark Ambor — Belong Together"
    track_id: 2mtubjs5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Mariah Carey — We Belong Together"
    track_id: 4pg0j5x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Flume — Say It"
    track_id: y3d3bjp    # scraped cue: 712s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Lil Wayne, Drake — Right Above It"
    track_id: uywbdjf    # scraped cue: 720s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Skrillex — Scary Monsters And Nice Sprites"
    track_id: 8lw9szx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Soulja Boy Tell 'Em — Turn My Swag On"
    track_id: 219mrlp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "DJ Snake — Turn Down For What"
    track_id: fzzsc6v5    # scraped cue: 793s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Chase, Status, Bou — Baddadan"
    track_id: 2jxyg4ff    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Chappell Roan — HOT TO GO!"
    track_id: pvfxm9w5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "JAY-Z — 99 Problems"
    track_id: 5c9mvq5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "The Chainsmokers — Roses"
    track_id: 47    # scraped cue: 873s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Route 94 — My Love"
    track_id: 2fg62l2p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Owl City — Fireflies"
    track_id: 13mpznkx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "D_EXC, Amél — Breathe Slow"
    track_id: 2qm7j7fx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Frenship — Capsize"
    track_id: 29vy41tf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Shaboozey — A Bar Song"
    track_id: pvjftwrp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Shaboozey — A Bar Song"
    track_id: 2qmkm0k5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Mac Miller — Best Day Ever"
    track_id: 2tu6tn9f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Black Eyed Peas — Don't Lie"
    track_id: b6zc203p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Leondis, Ferris — Nights Like This"
    track_id: 56    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "The Cranberries — Linger"
    track_id: p8tnkc1f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Billie Eilish — BIRDS OF A FEATHER"
    track_id: 3u03qz3f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Daft Punk — One More Time"
    track_id: j4rvgx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Sabrina Carpenter vs. Daft Punk — Espresso vs. One More Time"
    track_id: qg34g0zx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Daft Punk — One More Time"
    track_id: j4rvgx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Sabrina Carpenter — Espresso"
    track_id: 24x2ccl5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Alexandra Stan — Mr. Saxobeat"
    track_id: 1tubbh5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "PatFromLastYear — ID"
    track_id: 64    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Zach Bryan — Revival"
    track_id: g99g2z6f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Paramore — Misery Business"
    track_id: 22h4xd8p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Arcando, Tantron — So High"
    track_id: h58m8fhx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Fergie — London Bridge"
    track_id: txup0typ    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Hanumankind, Kalmi — Big Dawgs"
    track_id: glmrgy2x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Alice Deejay — Better Off Alone"
    track_id: 1pvst2lp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Gigi Perez — Sailor Song"
    track_id: 3jr05lhx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Alice Deejay — Better Off Alone"
    track_id: 11s66tsf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Avicii — Hey Brother"
    track_id: 2dhn8um5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "RetroVision — Feel Ur Luv"
    track_id: vps5jktp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "The Kid LAROI — Girls"
    track_id: rwqm0gpf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "NOTD, Riley Biederer — Under My Breath"
    track_id: mub8mlmx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Yeah Yeah Yeahs — Maps"
    track_id: 35b4f75    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Carly Rae Jepsen — Call Me Maybe"
    track_id: 8948zbf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "RetroVision, jeonghyeon — Friday Night"
    track_id: lnztrxrf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "DNCE — Cake By The Ocean"
    track_id: x4pygwf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Strange Talk — Young Hearts"
    track_id: tb6pn0p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Frankie Valli, The Four Seasons — December, 1963"
    track_id: 2f8udy5x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Gracie Abrams — That's So True"
    track_id: gvydxd5f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Knock2 — feel U luv Me"
    track_id: qbsufg5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Central Cee — BAND4BAND"
    track_id: g9925k4p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Two Friends — All For Me"
    track_id: 7wg4pw6f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Bastille — Pompeii"
    track_id: 1vqwlglp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Chappell Roan — Pink Pony Club"
    track_id: wuz6hnff    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Akon — Smack That"
    track_id: 18hktk55    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "The Isley Brothers — Shout"
    track_id: 5djc9tp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "DENNETT — Wild"
    track_id: 91    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Fred again.. — Jungle"
    track_id: 1sspnxbp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Chris Brown — Look At Me Now"
    track_id: 3nzpv85    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Meek Mill — Dreams And Nightmares"
    track_id: 20s98n2x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Dr. Dre — The Next Episode"
    track_id: 4ylcv8x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Sofi Tukker — Purple Hat"
    track_id: 2b21fq2f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Dr. Dre — The Next Episode"
    track_id: 2v63ugdf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Showtek, We Are Loud, Sonny Wilson, Ape Rave Club vs. Dr. Dre, Snoop Dogg, Kurupt, Nate Dogg — The Next Episode"
    track_id: 98    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Dr. Dre — The Next Episode"
    track_id: 1u06x2fp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Showtek — Booyah"
    track_id: 2v59sbzf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "2 Chainz — I'm Different"
    track_id: t81p44p    # scraped cue: 1732s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Deorro — Five More Hours"
    track_id: 37flj3sp    # scraped cue: 1776s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Lola Young — Messy"
    track_id: 157wu2bx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Naughty Boy — La La La"
    track_id: 28skmhkp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Eric Prydz — Call On Me"
    track_id: 7uslpup    # scraped cue: 1844s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Ganja White Night, Tape B — Ganja Tape"
    track_id: vdf5t1gf    # scraped cue: 1860s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Foreigner — Cold As Ice"
    track_id: 6ngc6rx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Daft Punk — Harder Better Faster Stronger"
    track_id: vhbbsf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Kelis — Milkshake"
    track_id: 5t7j8kx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Marshmello, Kane Brown — Miles On It"
    track_id: 2lwb9wz5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Galantis — Peanut Butter Jelly"
    track_id: 2vj2wrw5    # scraped cue: 1945s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Death Cab For Cutie — Soul Meets Body"
    track_id: 2rdk2u5p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Vluarr, Merow — Control"
    track_id: r9zxfqj5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Julian Jordan — I DON'T KNOW"
    track_id: 1zbjxzjx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Too $hort — Blow The Whistle"
    track_id: 1hqx5ms5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Two Friends — Trouble"
    track_id: cywfj765    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "The Foundations — Build Me Up Buttercup"
    track_id: s6krjsx    # scraped cue: 2095s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Clean Bandit — Rather Be"
    track_id: 20fg69rx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Dasha — Austin"
    track_id: qg25k9c5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Disco Lines — Disco Boy"
    track_id: glp4hmnx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Drake, Future — Big Rings"
    track_id: k0sm7dx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Dropkick Murphys — Shipping Up To Boston"
    track_id: 122    # scraped cue: 2194s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Martin Garrix, Bebe Rexha — In The Name Of Love"
    track_id: 15k5dshp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Post Malone — White Iverson"
    track_id: 2sqy0xzp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "BORGEOUS — Nobody"
    track_id: 26c9ctbp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Vanilla Ice — Ice Ice Baby"
    track_id: 8bgr29x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "George Michael — Careless Whisper"
    track_id: 2hr17pp    # scraped cue: 2286s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Casey Fitzpatrick — Sexy Sax"
    track_id: 128    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Crankdat, RUVLO — Lasers"
    track_id: usv5nbp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Charlem, American Dave — Engage!"
    track_id: rb1qtnj5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "GRiZ — Griztronics"
    track_id: 2r3dlsq5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Doechii — DENIAL IS A RIVER"
    track_id: 43d0h1bf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Edward Maya, Vika Jigulina — Stereo Love"
    track_id: 43f9mh9p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Edward Maya, Vika Jigulina — Stereo Love"
    track_id: 7yuqjzx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Rockwell — Somebody's Watching Me"
    track_id: rgcq1rf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Marshmello, Kane Brown — Miles On It"
    track_id: 2lwb9wz5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "FISHER — Losing It"
    track_id: 1ju5lzs5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "FISHER vs. Knock2 — Losing It"
    track_id: 138    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Knock2 — dashstar*"
    track_id: 1h5lvphx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "FISHER — Losing It"
    track_id: 1ju5lzs5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Disclosure — When A Fire Starts To Burn"
    track_id: w25u06x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Fall Out Boy — Sugar, We're Goin' Down"
    track_id: 1futm3mp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Eminem — Mockingbird"
    track_id: 1lw97wgx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "T-Pain — Buy U A Drank"
    track_id: 2k5bj01p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Capochino — Player One"
    track_id: 145    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Levity — Flip It"
    track_id: 2jyx817x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Ludacris — Get Back"
    track_id: 2km1b5p5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Neil Diamond — Sweet Caroline"
    track_id: mjzz94kx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Yellow Claw, Natte Visstick, RHYME — Crush"
    track_id: q4ub9yyx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Adam Port, Stryv — Move"
    track_id: h576nb8x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Rufus Wainwright — Hallelujah"
    track_id: 151    # scraped cue: 2760s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "BUNT., Nate Traveller — Clouds"
    track_id: p99t59p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Cloonee, InntRaw, Young M.A — Stephanie"
    track_id: qg48dv5f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Toni Basil — Mickey"
    track_id: 2jy23xyf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Kendrick Lamar — Bitch Don't Kill My Vibe"
    track_id: 1cw2k17p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Two Friends — No One Else"
    track_id: qg4rf595    # scraped cue: 2862s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Lucas, Steve — When You're Alone"
    track_id: 16sh3h0p    # scraped cue: 2932s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Tame Impala — The Less I Know The Better"
    track_id: 1lkslr85    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Eminem — The Monster"
    track_id: 1xmvl515    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Amél — Close To Me"
    track_id: km9hjpx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Big Mountain — Baby, I Love Your Way"
    track_id: 2f8uf50p    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Vluarr, Arcando — Ascend"
    track_id: lcq67dpp    # scraped cue: 3011s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "A$AP Rocky — Wild For The Night"
    track_id: g2fk6wp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Two Friends — More Than Yesterday"
    track_id: 1jbqtcz5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Morgan Wallen — Cowgirls"
    track_id: nq8bv4gp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Calvin Harris — This Is What You Came For"
    track_id: tc6ugwrf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Taylor Swift — I Can Do It With A Broken Heart"
    track_id: 1n4u6z35    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Bag Raiders — Shooting Stars"
    track_id: 38sdfqp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "FISHER — You Little Beauty"
    track_id: 1phltnt5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Jonas Brothers — Burnin' Up"
    track_id: 1653k3p5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Phoenix — 1901"
    track_id: 2535rupf    # scraped cue: 3164s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Phoenix — 1901"
    track_id: 6klww5f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Charli xcx — Apple"
    track_id: u74d8fkx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Waka Flocka Flame — Grove St. Party"
    track_id: 1cttblf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Sayo — Continuum"
    track_id: 175    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Arcade Fire — Wake Up"
    track_id: lctgfnyx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Lucenzo, Qwote — Danza Kuduro"
    track_id: 177    # scraped cue: 3240s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Lykke Li — I Follow Rivers"
    track_id: 94yqu1x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Red Hot Chili Peppers — Otherside"
    track_id: 9dnldx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "ILLENIUM, Dabin — Hearts On Fire"
    track_id: 1shm9m25    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Lewis Capaldi — Someone You Loved"
    track_id: 17lcwgwp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Two Friends — Way It Was"
    track_id: t1z94gz5    # scraped cue: 3312s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Avicii — Wake Me Up"
    track_id: f51kysf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Flux Pavilion, Wooli — On Repeat"
    track_id: 2k0tcmxx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Rick Ross — Hustlin'"
    track_id: 89uqu0x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "LMFAO — Party Rock Anthem"
    track_id: 7lf1yh5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Kajacks — I Can Do It"
    track_id: zmlvxg5    # scraped cue: 3478s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Michael Jackson — Thriller"
    track_id: vkb0v5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "My Chemical Romance — Teenagers"
    track_id: 1g2kuj2x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Tim Gordon, Bad Reputation — Your Move"
    track_id: 1nrun3tf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Doechii — Anxiety"
    track_id: mjznd5l5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Artemas — i like the way you kiss me"
    track_id: 1m71dcw5    # scraped cue: 3565s
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "The Neighbourhood — Sweater Weather"
    track_id: 244dn6tx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Lil Wayne — How To Love"
    track_id: 2fv94n4x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "James Bay — Let It Go"
    track_id: 195    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "David Guetta — Titanium"
    track_id: 1fyzf55    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Adele — Set Fire To The Rain"
    track_id: 416sxpp    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Twenty One Pilots — Tear In My Heart"
    track_id: nrtk415    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Kevin Rudolf — Let It Rock"
    track_id: h18cjx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Lana Del Rey — Summertime Sadness"
    track_id: 5xtp7np    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Alicia Keys — Girl On Fire"
    track_id: 2j6l0rkx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "MIMS — This Is Why I'm Hot"
    track_id: 8rb270f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Kings Of Leon — Sex On Fire"
    track_id: ud7d65    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Snoop Dogg — Drop It Like It's Hot"
    track_id: 74krtg5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Chappell Roan — HOT TO GO!"
    track_id: pvfxm9w5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Sean Kingston — Fire Burning"
    track_id: 2ryrfxff    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Harry Styles — Adore You"
    track_id: 1sdqu10f    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Ellie Goulding — Burn"
    track_id: 9gcf905    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "FISHER — You Little Beauty"
    track_id: 1phltnt5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Disclosure — When A Fire Starts To Burn"
    track_id: w25u06x    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "AJR — Burn The House Down"
    track_id: 1vzk4qlx    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Jonas Brothers — Burnin' Up"
    track_id: 1653k3p5    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
  - track: "Usher — Burn"
    track_id: 1086y4lf    # scraped cue: none
    version_tag:  # fill in: instrumental | acappella | full
    set_start_s:  # fill in
    set_end_s:    # fill in
    ref_start_s:  # fill in — seconds into ref where DJ dropped in (0 if from start)
    # is_loop: true          # uncomment if DJ loops/cuts the ref
    # ref_segments:          # then list iterations:
    #   - { ref_start_s: 0, ref_end_s: 0, mix_start_s: 0 }
