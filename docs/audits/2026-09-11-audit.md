# Weekly bug audit — 2026-09-11

Scope: `scripts/mac_analyze_loop.py`, `scripts/loop_prefetch.py`,
`scripts/loop_hardening.py`, `scripts/acquire_variant.py`,
`scripts/ingest_stem_url.py`, `scripts/replace_stem_audio.py`,
`scripts/replace_track_audio.py`, `scripts/mac_push_acquire.py`,
`analysis/gpu_worker.py`, `analysis/pipeline.py`,
`analysis/persistence.py`, `analysis/vast_worker.py`,
`ingest/main_retry.py`, `ingest/` (full, incl. `adapters/spotdl_adapter.py`,
`adapters/ytmusic_adapter.py`), `core/db.py`.

Note: `scripts/mert_backfill_loop.py`, `scripts/set_mert_backfill_loop.py`,
and `scripts/vast_loop.py` no longer exist in the repo — the three findings
they carried from the 2026-07-27 audit (3.3, 3.5, and one subsection of 3.4)
are hereby CLOSED by deletion.

Bug families: A = encoding/mojibake · B = missing timeouts/integrity checks ·
C = silent-failure loops (poison-pill retry, dishonest exit codes, except:pass).

---

## 1. Entropy snapshot

```
python scripts/entropy_audit.py --snapshot
{
  "net_subprocess_no_timeout": 16,
  "net_subprocess_no_encoding": 8,
  "bare_except": 0
}

bug-class fences:
  net_subprocess_no_timeout          16  (baseline 30)
  net_subprocess_no_encoding          8  (baseline 25)
  bare_except                         0  (baseline 0)

python scripts/entropy_audit.py --check
entropy_audit: OK
```

All metrics are well below their baselines — net improvement since the ratchet
was set. No regression. `--check` passed clean.

---

## 2. Findings closed since last audit (2026-07-27)

| Finding | Resolution |
|---------|------------|
| LOW C2 — backfill loops no `register_transient` (`mert_backfill_loop.py`, `set_mert_backfill_loop.py`) | **CLOSED** — both files deleted from repo |
| LOW B — `set_mert_backfill_loop.push_set_mert_rows` split transaction | **CLOSED** — file deleted |

---

## 3. Open findings

### 3.1 HIGH · C (carried) — `gpu_worker.py` increments `n_done` on failure, always returns 0

**File:** `analysis/gpu_worker.py` · Lines 359–372

**Severity upgrade from MEDIUM → HIGH** — this is the sole GPU-batch entry
point and runs on rented hardware with a cost target; silent all-fail with
exit 0 wastes the entire budget with no signal.

`n_done += 1` fires on both the success path (line 369) and the failure
`continue` path (line 364). Both `return 0` exits (lines 337 and 372) are
unconditional. Consequences:

1. A GPU run where every track fails logs "done (processed N, quarantined 0)"
   and exits 0 — CI/scheduler treats it as success.
2. `--max-tracks N` is driven by `n_done`; ten consecutive failures exhaust the
   limit with zero analysis records written, exit 0.
3. `_load_audio_asset` failures at lines 353–357 add to `failed_tids` but are
   not counted anywhere — they vanish from the final drain log entirely.

Compare `mac_analyze_loop.py` which calls `exit_status(done, failed, pf)` and
escalates to ERROR + nonzero when `n_done == 0` or failure ratio is high.

**Fix:** increment a separate `n_failed` counter on the failure path; call
`loop_hardening.exit_status(n_done, n_failed)` at both return sites; tally
`_load_audio_asset` misses separately so the drain log is honest.

---

### 3.2 MEDIUM · C (carried, upgraded) — `mac_analyze_loop.py _persist_in_bg` catches only `CalledProcessError`

**File:** `scripts/mac_analyze_loop.py` · Lines 409–424

```python
except subprocess.CalledProcessError as e:
    counts.persist_failed_inc()
    log.error("[%d] (bg) push failed: %s", tid, e)
```

`subprocess.TimeoutExpired` is a sibling of `CalledProcessError` (both inherit
`SubprocessError`), not a subclass. If `rsync_stems_out` or `push_track_rows`
times out, the exception escapes the handler, `persist_failed_inc()` is never
called, and the background thread terminates silently. The main loop's
`bg.join()` returns normally; `exit_status()` then sees an understated
`n_persist_failed` and may return 0 for a run where results were never written
to the canonical DB.

Previously graded LOW as "exception scope too narrow"; upgrading to MEDIUM
because `TimeoutExpired` is a realistic event on a flaky Tailscale connection
and the consequence (silent persist failure counts as success) is the same
class-C pattern as finding 3.1.

**Fix:** widen to `except subprocess.SubprocessError` or add a separate
`except subprocess.TimeoutExpired` arm calling `persist_failed_inc()`.

---

### 3.3 LOW (carried) — `mac_analyze_loop.py` sha256 `OSError` guard missing

**File:** `scripts/mac_analyze_loop.py` · Lines ~454–463

```python
if asset.sha256:
    local_sha = sha256_file(local_audio)   # no try/except OSError
    if local_sha != asset.sha256:
```

An `OSError` (file truncated, NFS stale handle) propagates uncaught, killing
the analysis thread for that track and leaving `persist_failed_inc()` uncalled.
`loop_prefetch.py` guards the equivalent call; `mac_analyze_loop.py` does not.

---

### 3.4 LOW (new) · B — `mac_analyze_loop.py` rsync calls missing SSH keepalive opts

**File:** `scripts/mac_analyze_loop.py` · Lines ~178, ~199 (`rsync_in`,
`rsync_stems_out`)

Both rsync calls do not pass `-e "ssh <SSH_OPTS>"`. `SSH_OPTS` in
`loop_hardening.py` includes `ServerAliveInterval=15 ServerAliveCountMax=3`,
which kills a dead Tailscale pipe in ~45 s. Without it, rsync's internal SSH
session has no keepalive and can hang indefinitely on a dead link — the
wall-clock subprocess `timeout=RSYNC_TIMEOUT` (600 s) is the only backstop.

Impact: bounded by the 600 s timeout, so this is latent, not a blocker. But the
gap means a Tailscale reconnect (common in mobile home office setups) silently
blocks for up to 10 min per rsync instead of recovering in 45 s.

---

### 3.5 LOW (new) · A — `replace_track_audio.py` `text=True` without `encoding=`

**File:** `scripts/replace_track_audio.py` · Line 650

```python
out = subprocess.run(
    [..., "ffprobe", ...],
    capture_output=True,
    text=True,         # ← no encoding=
    timeout=10,
)
```

ffprobe output is ASCII numerics in practice; this is latent/hardening. Fix:
add `encoding="utf-8"` to match `loop_hardening.TEXT_KW`.

---

### 3.6 LOW (new) · A — `analysis/persistence.py` `.decode()` without explicit encoding

**File:** `analysis/persistence.py` · Line 83

```python
subprocess.check_output(["git", "rev-parse", ...], stderr=subprocess.DEVNULL)
.decode()
```

Git short SHA is always hex ASCII so Latin-1 vs UTF-8 is moot in practice;
marked latent/hardening. Fix: add `encoding="utf-8"` or use
`text=True, encoding="utf-8"`.

---

### 3.7 HIGH (new) · A — `ingest/adapters/spotdl_adapter.py` `text=True` without `encoding=`

**File:** `ingest/adapters/spotdl_adapter.py` · Lines 99, 232

**Status: confirmed present**

Both `download_one` and `download_batch` call:

```python
proc = subprocess.run(cmd, capture_output=True, text=True, timeout=timeout_s)
```

`text=True` is used without `encoding=`. Python falls back to
`locale.getpreferredencoding()`, which is Latin-1 on pi-storage (the
`loop_hardening.py` header confirms this explicitly). spotdl routinely prints
artist/title strings to stdout/stderr containing non-ASCII characters (é, ö, ñ,
etc.). On a Latin-1 locale these either silently mangle or raise
`UnicodeDecodeError`. That exception is **not** caught by the surrounding
`except subprocess.TimeoutExpired / except OSError` clauses — it propagates
uncaught through `_process_track` / `_attempt`, crashing the per-track call
instead of returning a clean `Err`. `loop_hardening.TEXT_KW` covers this pattern
but is never imported or applied in the adapter files.

**Fix:** pass `encoding="utf-8", errors="replace"` on both subprocess.run calls
(matches `loop_hardening.TEXT_KW`).

---

### 3.8 MEDIUM (new) · A — `ingest/adapters/ytmusic_adapter.py` `text=True` without `encoding=`

**File:** `ingest/adapters/ytmusic_adapter.py` · Line 216

**Status: confirmed present**

`_ytdlp_download` has the same pattern — `text=True` without `encoding=`. yt-dlp
runs with `--quiet` so stdout is minimal, but error-path stderr messages can
include the video title from YouTube metadata, which may contain non-ASCII bytes.
Same uncaught-`UnicodeDecodeError` failure mode as §3.7, lower probability of
triggering.

**Fix:** add `encoding="utf-8", errors="replace"`.

---

### 3.9 MEDIUM (new) · C — `ingest/main.py` unconditional `return 0`

**File:** `ingest/main.py` · Line 617

**Status: confirmed present**

`_run` ends with an unconditional `return 0` after `_summarize(stats, ...)`.
`RunStats` tracks meaningful failure buckets (`failed_download`, `failed_db`,
etc.) but none are consulted before the return. A complete download failure
(`downloaded=0, failed_download=300`) exits 0; systemd/monitoring treats a
fully-broken corpus run as success. `loop_hardening.exit_status` covers this
exact pattern but is not imported or called here. (Early-return error paths at
lines ~450 and ~459 correctly return 1 — only the normal drain is wrong.)

**Fix:** call `exit_status(stats.downloaded, stats.failed_download + stats.failed_db)`
or equivalent before returning.

---

### 3.10 MEDIUM (new) · C — `ingest/main_retry.py` unconditional `return 0`

**File:** `ingest/main_retry.py` · Line 417

**Status: confirmed present**

Same pattern as §3.9. `RunStats` in `main_retry.py` tracks `failed_timeout`,
`failed_other`, `db_failed` — all nonzero after a spotdl rate-limit wipeout —
but `_run` returns 0 unconditionally. A complete spotdl failure (200 timeouts, 0
downloads) exits 0.

**Fix:** check failure counts before returning and call `sys.exit(1)` / return 1
when `downloaded == 0 and (failed_timeout + failed_other) > 0`.

---

### 3.11 LOW (new) · C — `ingest/main.py` `db_failed` status not tracked in `RunStats`

**File:** `ingest/main.py` · Lines 607–609

**Status: confirmed present**

The per-track status dispatch handles `"db_failed"` with a log line but does not
increment any counter in `RunStats`. DB failures are therefore invisible in
`_summarize` output and, if `exit_status` is added (§3.9), they would still be
excluded from the failure ratio.

---

## 4. Priority worklist

| Priority | Severity | Finding | Owner hint |
|----------|----------|---------|------------|
| **1** | HIGH | `gpu_worker.py` `n_done`/`return 0` (§3.1) | Fix before next GPU box run — dishonest exit on rented hardware |
| **2** | HIGH | `spotdl_adapter.py` `text=True` without encoding (§3.7) | Real crash on pi-storage with non-ASCII track names — add `encoding="utf-8", errors="replace"` to both calls |
| 3 | MEDIUM | `mac_analyze_loop.py _persist_in_bg` exception scope (§3.2) | Widen to `SubprocessError` |
| 4 | MEDIUM | `ytmusic_adapter.py` `text=True` without encoding (§3.8) | Same fix as §3.7 |
| 5 | MEDIUM | `ingest/main.py` unconditional `return 0` (§3.9) | Wire `exit_status` |
| 6 | MEDIUM | `ingest/main_retry.py` unconditional `return 0` (§3.10) | Wire exit code check |
| 7 | LOW | `mac_analyze_loop.py` sha256 `OSError` guard (§3.3) | One-liner try/except |
| 8 | LOW | `mac_analyze_loop.py` rsync SSH_OPTS gap (§3.4) | Add `-e "ssh ..."` to both rsync calls |
| 9 | LOW | `ingest/main.py` `db_failed` uncounted (§3.11) | Add `db_failed` field to `RunStats` |
| 10 | LOW | `replace_track_audio.py` `text=True` (§3.5) | Add `encoding="utf-8"` |
| 11 | LOW | `persistence.py` `.decode()` (§3.6) | Add `encoding="utf-8"` |
