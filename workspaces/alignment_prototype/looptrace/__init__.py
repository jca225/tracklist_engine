"""looptrace — non-monotonic alignment of DJ-mix acappella spans.

Phased effort (see NOTES.md): ill-posedness audit -> loop-collapse ->
landmark point cloud + fixed-slope segment extraction. Runs side-by-side
with the legacy `path_decode` decoder; never imported by it.
"""
