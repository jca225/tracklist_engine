# Digs: bb11_39 fp wiring + bb12_3w2 soft miss

Date: 2026-07-18  
Branch: `fp-hit-decoder-clean`

## bb11_39 — fp *did* fire; belief discarded it

Agentic driver events for span `39` (pre-fix timeline):

| probe | set_start | note |
|---|---|---|
| fp | **3399.8** | GT is 3398.9 — correct |
| mert_decode | 3459.1 | |
| surprise | 3475.1 | |
| cue_prior | 3481.0 | **final start_source** |

So this is **not** a missing-fp wiring bug. Fp ran live; cue/surprise won the
pre-fix belief vote. The competitive-fp cluster preference (`FP_CLUSTER_MARGIN`)
should recover 3399.8 on re-race. Classical race timeline lacked fp in
`probe_proposals` (stale vs `_lt` / predicted), but agentic events prove the
probe path works.

## bb12_3w2 — soft miss (~10s), not an overwrite of a GT mode

| source | set_start | err to GT 271.9 |
|---|---|---|
| classical / `_lt` fp | 282.1 | **+10.2s** |
| agentic surprise | 323.9 | +52s (regression on top) |
| mode_audit nearest | 282.1 | `gt_absent` (threshold 6s) |

Vote-argmax cluster *starts* at 282s with huge votes (1967) — no top‑K candidate
near 271.9. Density cannot invent an earlier onset; the landmark mass begins
~10s after audible entry (likely fade-in / weak early hashes). Belief fix should
at least stop agentic from making it **worse** (keep ~282 vs 324). True onset
recovery needs a different lever (onset/envelope, or vote-extent left edge with
a lead pad) — out of scope for the density/belief pass.
